apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lab3-gitops
  namespace: argocd
spec:
  project: default
  source:
    # 👇👇👇 PON AQUÍ TU URL DE GITHUB (Asegúrate que el repo sea PÚBLICO) 👇👇👇
    repoURL: https://github.com/JhomarArrieta/AS-lab3-Rest-HATEOAS-Docker-MySQL-Kubernetes.git
    targetRevision: HEAD
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true